package main	// package utama, dimana program akan dijalankan dari package ini
import "fmt"	// mengimport package fmt, yang berisi fungsi untuk melakukan format input dan output

// main function, fungsi utama yang akan dijalankan pertama kali
func main() {
	fmt.Println("Hello, World!");	// mencetak tulisan "Hello, World!" ke layar
}